#ifndef RENAME_H
#define RENAME_H

#include <QWidget>

class Rename : public QWidget
{
    Q_OBJECT
public:
    explicit Rename(QWidget *parent = 0);
    ~Rename();

signals:

public slots:
};

#endif // RENAME_H
