#ifndef RENAME_H
#define RENAME_H

#include <QMainWindow>
#include <QWidget>

class Rename : public QMainWindow
{
    Q_OBJECT
public:
    explicit Rename(QMainWindow *parent = 0);
    ~Rename();

signals:

public slots:
};

#endif // RENAME_H
